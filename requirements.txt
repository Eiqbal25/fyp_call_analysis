# ─────────────────────────────────────────────────────────────
# FYP1 Call Analysis System — Requirements
# Install with: pip install -r requirements.txt
#
# Tested on Python 3.12 (Conda environment: fyp2)
#
# NOTE: PyTorch is managed by Conda (CUDA 12.1).
#       Do NOT install torch via pip if using the Conda GPU environment.
#       To install PyTorch manually (CPU only):
#         pip install torch torchvision torchaudio
# ─────────────────────────────────────────────────────────────

# ── Audio Processing ─────────────────────────────────────────
librosa>=0.10.0
soundfile>=0.12.1
noisereduce>=3.0.0
numpy>=1.24.0

# ── Automatic Speech Recognition ─────────────────────────────
openai-whisper>=20231117

# ── Speaker Diarization (Primary — neural, GPU-accelerated) ──
pyannote.audio>=3.1.0

# ── Speaker Diarization (Fallback + fix_speakers.py) ─────────
resemblyzer>=0.1.1.dev0

# ── Machine Learning ─────────────────────────────────────────
# torch>=2.0.0   <-- Managed by Conda (CUDA 12.1). Do not pip install.
scikit-learn>=1.3.0
scipy>=1.10.1

# ── Sentiment Analysis ───────────────────────────────────────
vaderSentiment>=3.3.2

# ── Data Handling ─────────────────────────────────────────────
pandas>=2.0.0

# ── Visualization ─────────────────────────────────────────────
matplotlib>=3.7.0
plotly>=5.15.0

# ── Dashboard ────────────────────────────────────────────────
streamlit>=1.40.0

# ── Utilities ────────────────────────────────────────────────
tqdm>=4.65.0

# ── LLM API (Method 3 — Groq + Llama 3.3 70B) ────────────────
groq>=0.4.0

# ── Environment Variables ─────────────────────────────────────
python-dotenv>=1.0.0

# ── HuggingFace (for pyannote model download) ─────────────────
huggingface_hub>=0.20.0
